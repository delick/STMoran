运行Workflow.m即可。
Moran.m, Blocking.m为依赖函数，workflow会调用这两个函数，所以尽量在同一个文件夹下比较好。

输入参数：
掩膜：使用FinelRice0915即可。
原始影像：输入至少两个时期的NDRE影像。（仅输入一个影像会报错）

输出参数：
Moran的统计值，从左到右排列。格式如下

		第一幅影像		|		第二幅影像		|
n等分 | 均值 | 标准差 | 最大值 | 最小值	|		…………………………		|

Greay's C还在写……
